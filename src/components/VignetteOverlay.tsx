// Removed - vignette overlays were creating an artificial darkening effect
// that could feel like a visual bug. If needed in future, use very subtle
// top-only gradient on scroll.

export const VignetteOverlay = () => {
  // Return nothing - effect removed for cleaner UX
  return null;
};

export default VignetteOverlay;
